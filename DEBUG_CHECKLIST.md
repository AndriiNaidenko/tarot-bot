# 🔍 Чеклист для отладки Railway

## Пожалуйста, проверьте следующее и пришлите информацию:

### 1. Статус деплоя
В Railway Dashboard → CHACK-taro → Deployments:
- [ ] Какой статус? (Building / Running / Crashed / Failed)
- [ ] Если Failed - скопируйте текст ошибки

### 2. Переменные окружения
В Railway Dashboard → CHACK-taro → Variables:

**Проверьте, что добавлены:**
- [ ] `TELEGRAM_BOT_TOKEN` = `8551518470:AAG6AbFJwSwqphvIu_xIDHQ4N0v2eO3mEkg`
- [ ] `EMERGENT_LLM_KEY` = `sk-emergent-7E038Ff1767408e4eF`
- [ ] `MONGO_URL` = (должен быть URL к MongoDB)
- [ ] `DB_NAME` = `tarot_bot`

### 3. Логи сервисов
Откройте логи для каждого сервиса и скопируйте последние 20-30 строк:

**Сервис: web (main.py)**
```
[скопируйте логи сюда]
```

**Сервис: worker (channel_poster.py)**
```
[скопируйте логи сюда]
```

### 4. Структура проекта
Проверьте в Railway → Settings → GitHub:
- [ ] Репозиторий: `AndriiNaidenko/CHACK`
- [ ] Ветка: `main`
- [ ] Root Directory: `/` или пустое (не должно быть /tarot-bot)

### 5. Procfile
Убедитесь, что Railway видит Procfile:
- [ ] В логах должно быть "Detected Procfile" или "Running: web" / "Running: worker"

---

## Частые проблемы и решения:

### ❌ "Module not found" или "ImportError"
**Решение:** Проверьте, что requirements.txt установлен:
```
aiogram>=3.22.0
motor>=3.3.2
emergentintegrations
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
aiohttp>=3.12.0
apscheduler>=3.10.0
```

### ❌ "MongoDB connection failed"
**Решение:** Добавьте MongoDB сервис в Railway или проверьте MONGO_URL

### ❌ "Unauthorized" или "Invalid bot token"
**Решение:** Проверьте TELEGRAM_BOT_TOKEN в Variables

### ❌ "No module named 'backend'"
**Решение:** Убедитесь, что Root Directory пустой (не /tarot-bot)

### ❌ Railway не запускает worker
**Решение:** Railway может запускать только один процесс. Нужно создать два отдельных сервиса:
1. Service #1: web → start command: `python main.py`
2. Service #2: worker → start command: `python channel_poster.py`

---

## 🆘 Если ничего не помогает:

Пришлите мне:
1. ✅ Скриншот Dashboard с сервисами
2. ✅ Скриншот Variables
3. ✅ Полный текст логов (последние 50 строк)

И я быстро найду проблему! 🔍
