# 🔍 Отладка ошибок Railway

## Как проверить логи:

1. Откройте Railway проект: https://railway.app/project/677136b1-9c3e-4a1b-b7f6-50ad99ffd9c3
2. Кликните на сервис (Main Bot)
3. Откройте вкладку **Deployments**
4. Кликните на последний деплой
5. Откройте **View Logs**

---

## 🚨 Частые ошибки и решения:

### ❌ "ModuleNotFoundError: No module named 'backend'"

**Причина:** Root Directory установлен неправильно

**Решение:**
- Settings → Source → **Root Directory:** оставьте **ПУСТЫМ** (удалите /tarot-bot если есть)
- Redeploy

---

### ❌ "Field required: TELEGRAM_BOT_TOKEN"

**Причина:** Переменная окружения не добавлена

**Решение:**
Добавьте в Variables:
```
TELEGRAM_BOT_TOKEN=8551518470:AAG6AbFJwSwqphvIu_xIDHQ4N0v2eO3mEkg
EMERGENT_LLM_KEY=sk-emergent-7E038Ff1767408e4eF
MONGO_URL=<из MongoDB сервиса>
DB_NAME=tarot_bot
```

---

### ❌ "Connection refused: MongoDB"

**Причина:** MONGO_URL неправильный или MongoDB не добавлен

**Решение:**
1. В проекте добавьте MongoDB: **"+ New"** → **"Database"** → **"Add MongoDB"**
2. Откройте MongoDB сервис → вкладка **Variables**
3. Скопируйте значение `MONGO_URL`
4. Вставьте в Variables основного сервиса

---

### ❌ "Invalid token" или "Unauthorized"

**Причина:** Токен Telegram бота неправильный

**Решение:**
1. Откройте @BotFather в Telegram
2. Отправьте `/token`
3. Выберите @taro208_bot
4. Скопируйте новый токен
5. Обновите `TELEGRAM_BOT_TOKEN` в Variables

---

### ❌ "emergentintegrations not found"

**Причина:** Библиотека не установилась

**Решение:**
Проверьте в логах:
```
Installing dependencies from requirements.txt
...
Successfully installed emergentintegrations-0.1.0
```

Если нет - проблема с `requirements.txt`. Проверьте что файл содержит:
```
emergentintegrations
```

---

### ❌ "ConflictError: Terminated by other getUpdates"

**Причина:** Бот запущен в двух местах одновременно

**Решение:**
1. Остановите локальную версию бота (если запущена)
2. Убедитесь, что только один сервис в Railway запускает `main.py`

---

### ❌ Build fails: "No Procfile, buildpack not detected"

**Причина:** Railway не может определить как запустить проект

**Решение:**
- Settings → Deploy → **Start Command:** `python main.py`
- Или создайте `railway.json` (уже есть в репозитории)

---

### ❌ "Cannot find module data/tarot_cards.json"

**Причина:** Файл данных не в репозитории

**Решение:**
Проверьте что в репозитории есть:
```
data/
└── tarot_cards.json
```

---

## 📋 Пришлите мне эту информацию:

Если ошибка не в списке выше, скопируйте из логов:

1. **Последние 30 строк логов** (где ERROR или FAILED)
2. **Скриншот Variables** (проверить что все 4 переменные добавлены)
3. **Settings → Source** (Root Directory должен быть пустой)
4. **Settings → Deploy** (Start Command должен быть `python main.py`)

И я сразу скажу что исправить! 🔍

---

## ✅ Как должны выглядеть успешные логи:

```
[Build]
✅ Detected Python 3.11
✅ Installing dependencies from requirements.txt
✅ Collecting aiogram>=3.22.0
✅ Successfully installed aiogram-3.22.0 motor-3.3.2 ...

[Deploy]
✅ 2025-11-25 - backend.database - INFO - MongoDB connected: tarot_bot
✅ 2025-11-25 - __main__ - INFO - 🔮 Tarot Bot starting...
✅ 2025-11-25 - aiogram.dispatcher - INFO - Run polling for bot @taro208_bot
```

Если видите это - бот работает! 🎉
