# ✅ Проверка настроек Railway

## 📋 Вы добавили переменные - отлично!

Вижу на скриншоте что добавлены 5 переменных. Отлично! ✅

Но деплой падает (FAILED). Давайте проверим остальные настройки.

---

## 🔍 Что нужно проверить:

### 1. Откройте сервис "tarot-bot"

Кликните на сервис **tarot-bot** (не MongoDB)

### 2. Проверьте Settings → Source

**Должно быть:**
- ✅ **Source Repo:** `AndriiNaidenko/tarot-bot`
- ✅ **Branch:** `main`
- ✅ **Root Directory:** **ПУСТОЕ** (не `/tarot-bot`, не `/backend`)

**Если Root Directory НЕ пустое:**
- Удалите всё из этого поля
- Сохраните
- Redeploy

### 3. Проверьте Settings → Deploy

**Должно быть:**
- ✅ **Start Command:** `python main.py`

**Если там что-то другое или пусто:**
- Введите: `python main.py`
- Сохраните
- Redeploy

### 4. Проверьте Variables

Убедитесь что все 5 переменных:
- ✅ `TELEGRAM_BOT_TOKEN`
- ✅ `OPENAI_API_KEY`
- ✅ `EMERGENT_LLM_KEY` (можно удалить, не используется)
- ✅ `MONGO_URL`
- ✅ `DB_NAME`

### 5. Проверьте логи

Откройте **Deployments** → последний FAILED → **View Logs**

**Найдите строку с ERROR или причину падения**

---

## 🚨 Частые причины FAILED:

### ❌ "No module named 'backend'"
**Причина:** Root Directory не пустой

**Решение:** Settings → Source → Root Directory → удалите всё

---

### ❌ "Field required: TELEGRAM_BOT_TOKEN"
**Причина:** Переменные не применились

**Решение:** Перезайдите в Variables, проверьте что все на месте, сделайте Redeploy

---

### ❌ "MongoServerError: bad auth"
**Причина:** MONGO_URL неправильный

**Решение:**
1. Откройте сервис **MongoDB**
2. Вкладка **Connect**
3. Скопируйте правильный `MONGO_URL`
4. Обновите в tarot-bot Variables
5. Redeploy

---

### ❌ "requirements.txt not found"
**Причина:** Репозиторий не тот или Root Directory установлен

**Решение:**
- Проверьте что Source Repo = `AndriiNaidenko/tarot-bot`
- Root Directory должен быть пустой

---

## 📸 Пришлите скриншоты:

Если не помогло, пришлите:

1. **Settings → Source** (покажет Root Directory)
2. **Settings → Deploy** (покажет Start Command)
3. **Логи** (View Logs → последние 30 строк)

И я сразу скажу что исправить! 🔍

---

## ✅ Как должны выглядеть успешные логи:

```
[Build]
✅ Installing dependencies from requirements.txt
✅ Successfully installed aiogram-3.22.0 motor-3.3.2 ...

[Deploy]
✅ MongoDB connected: tarot_bot
✅ 🔮 Tarot Bot starting...
✅ Run polling for bot @taro208_bot
```

---

## 🔐 Про GitHub и OpenAI ключи:

**Вопрос:** Почему GitHub блокирует OpenAI ключи?

**Ответ:** Это **GitHub Secret Scanning** - защита от утечки API ключей. GitHub автоматически сканирует все коммиты и блокирует пуш если находит API ключи (OpenAI, AWS, etc).

**Это правильно! ✅** API ключи никогда не должны быть в git репозитории.

**Правильный подход (который мы используем):**
- ✅ Ключи хранятся в Railway Variables (безопасно)
- ✅ В репозитории только `.env.example` с плейсхолдерами
- ✅ Реальный `.env` в `.gitignore` (не попадает в git)

Так что всё работает как задумано! 🎉

---

**Проверьте Settings и пришлите логи если нужна помощь!**
